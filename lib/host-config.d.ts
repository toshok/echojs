// declarations for the GENERATED lib/host-config.js (see
// host-config.js.in and the //lib:host-config.js genrule)
export const LLVM_SUFFIX: string;
export const LLVM_BINDIR: string;
// major version of the LLVM the compiler was built against ("22");
// the driver refuses to spawn a different major's opt/llc
export const LLVM_MAJOR: string;
export const RUNLOOP_IMPL: string;

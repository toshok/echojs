
machine:
  services:
    - docker

dependencies:
  override:
    - docker pull ubuntu:14.04.1
    - docker info
    - docker run -v /home/ubuntu:/echojs -d ubuntu:14.04.1 -a /echojs/ci/linux/circleci.sh

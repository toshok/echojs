
machine:
  services:
    - docker

dependencies:
  override:
    - docker pull ubuntu:14.04.1
    - docker info
    - docker run -v /home/ubuntu:/echojs --name ejs-ubuntu -d ubuntu /echojs/ci/linux/circleci.sh

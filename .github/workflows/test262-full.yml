# The full test262 suite, sharded across parallel runners.  Distinct
# from the curated lane in bootstrap.yml: the lane is a fast per-PR gate
# (a strided selection checked against expectations.txt); this runs
# every language/ + built-ins/ + harness/ test and reports a pass-rate
# summary — comprehensive coverage, not a gate.
#
# One build job assembles the stage1 workroot and uploads it; N shard
# jobs each run their slice against that same workroot (the compiler is
# built once, not N times); a collect job concatenates the shard results
# and posts the report to the run summary.  macOS only — JS semantics
# don't vary by platform (the bootstrap matrix covers the parts that do).
name: test262-full

on:
  schedule:
    - cron: "0 8 * * *" # nightly, 08:00 UTC
  workflow_dispatch:
    inputs:
      shards:
        description: number of parallel shards
        default: "8"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  build:
    name: build workroot
    runs-on: macos-15 # arm64
    timeout-minutes: 90
    outputs:
      shards: ${{ steps.cfg.outputs.shards }}
      shard-list: ${{ steps.cfg.outputs.list }}
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install llvm
        run: brew install llvm

      - uses: dtolnay/install-buck2@latest

      - uses: actions/setup-node@v4
        with:
          node-version: 22.x

      - name: npm ci
        run: npm ci

      - name: Build the node-llvm addon
        run: ./node-llvm/build-addon.sh "$(brew --prefix llvm)"

      # the workroot the shard jobs run against: srcdir-tree +
      # lib/generated + the stage1 compiler, tarred whole (cp -RL has
      # already dereferenced the buck-out symlinks)
      - name: assemble workroot
        run: |
          ./test/test262/assemble-workroot.sh "$RUNNER_TEMP/workroot"
          tar -C "$RUNNER_TEMP/workroot" -czf workroot.tar.gz .

      - uses: actions/upload-artifact@v4
        with:
          name: test262-workroot
          path: workroot.tar.gz
          if-no-files-found: error

      # publish the shard count (N) and the matrix list ([0..N-1]) so
      # the shard job's --shard K/N and its matrix agree
      - id: cfg
        run: |
          N="${{ github.event.inputs.shards || '8' }}"
          echo "shards=$N" >> "$GITHUB_OUTPUT"
          echo "list=$(node -e 'console.log(JSON.stringify([...Array(+process.argv[1]).keys()]))' "$N")" >> "$GITHUB_OUTPUT"

  shard:
    name: shard ${{ matrix.shard }}
    needs: build
    runs-on: macos-15
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        shard: ${{ fromJSON(needs.build.outputs.shard-list) }}
    steps:
      # the runner scripts + the pinned suite SHA (no submodules — the
      # compiler is prebuilt in the workroot artifact)
      - uses: actions/checkout@v4

      - name: Install llvm
        run: brew install llvm # the compiler shells out to opt/llc/clang++

      - uses: actions/setup-node@v4
        with:
          node-version: 22.x

      - uses: actions/download-artifact@v4
        with:
          name: test262-workroot

      - name: unpack workroot
        run: |
          mkdir -p "$RUNNER_TEMP/workroot"
          tar -C "$RUNNER_TEMP/workroot" -xzf workroot.tar.gz

      - name: checkout test262
        run: |
          git init -q "$RUNNER_TEMP/test262"
          git -C "$RUNNER_TEMP/test262" fetch -q --depth 1 \
            https://github.com/tc39/test262.git "$(cat test/test262/suite.sha)"
          git -C "$RUNNER_TEMP/test262" checkout -q FETCH_HEAD

      # LLVM is left to the driver's own discovery (baked major + brew),
      # exactly as the bootstrap job's lane step relies on
      - name: run shard
        run: |
          node test/test262/run-test262.mjs run \
            --suite "$RUNNER_TEMP/test262" \
            --ejs "$RUNNER_TEMP/workroot" \
            --jobs 4 \
            --stride-language 1 --cap-builtins all \
            --shard ${{ matrix.shard }}/${{ needs.build.outputs.shards }} \
            --out "shard-${{ matrix.shard }}.jsonl"

      - uses: actions/upload-artifact@v4
        with:
          name: test262-shard-${{ matrix.shard }}
          path: shard-${{ matrix.shard }}.jsonl
          if-no-files-found: error

  report:
    name: collect + report
    needs: shard
    if: always() && needs.shard.result != 'cancelled'
    runs-on: macos-15
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22.x

      - uses: actions/download-artifact@v4
        with:
          pattern: test262-shard-*
          merge-multiple: true
          path: shards

      - name: report
        run: |
          cat shards/*.jsonl > all.jsonl
          node test/test262/run-test262.mjs report --in all.jsonl --md report.md
          cat report.md >> "$GITHUB_STEP_SUMMARY"

      - uses: actions/upload-artifact@v4
        with:
          name: test262-full-results
          path: |
            all.jsonl
            report.md
          if-no-files-found: error

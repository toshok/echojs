// generator: none
// xfail: we test the Date() function, which returns the current time.  this differs from the current time when the node test runs.  bleah.

console.log(Date());
console.log(new Date());

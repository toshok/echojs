// generator: esm

// `.js`-suffixed specifiers (NodeNext output style) key the same module
// as the suffix-free form — one module, one copy of its state.
import { V } from "./jsimport1-lib.js";
import { inc } from "./jsimport1-lib";

console.log(V);
console.log(inc(V));
